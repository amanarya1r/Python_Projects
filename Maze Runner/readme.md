# Maze Runner 
It's a terminal based game where user needs to find the shortest path to run through the maze. It uses curses library also this project is more like a practice of using curses library to make better terminal app.

>**Note:** You need curses library to run this terminal game.